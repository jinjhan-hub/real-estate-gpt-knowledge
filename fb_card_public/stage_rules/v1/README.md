# A-1R-3 GitHub 覆蓋包

本包已依照 GitHub 實際路徑整理：

```txt
fb_card_public/stage_rules/v1/05_final_image_prompt.md
fb_card_public/stage_rules/v1/06_image_generation_failsafe.md
```

使用方式：

1. 解壓縮本 zip。
2. 將 `fb_card_public` 資料夾上傳到 GitHub repository 根目錄。
3. 覆蓋既有檔案：
   - `05_final_image_prompt.md`
   - `06_image_generation_failsafe.md`

本包不包含圖片、URL、base64，也未修改 API route.ts。
